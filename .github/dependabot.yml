version: 2

updates:
  # For GitHub Actions (if you add later)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Future-proof: npm (if you add JS packages later)
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
